<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tool="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/character_image_iv"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:padding="10dp"
        tool:src="@mipmap/ic_placeholder"
        app:layout_constraintBottom_toTopOf="@id/character_name_tv"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/character_name_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#fff"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@id/character_image_iv"
        app:layout_constraintStart_toStartOf="@id/character_image_iv"
        app:layout_constraintTop_toBottomOf="@id/character_image_iv"
        tool:text="MissingNo." />
</androidx.constraintlayout.widget.ConstraintLayout>